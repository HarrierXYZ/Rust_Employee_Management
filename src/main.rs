/*Goal: Write a program that manages employees in different departments of a company.
You will use a HashMap to store the departments and the employees within them. */


use std::io;
use std::collections::HashMap;

let mut company_employees: HashMap<String, Vec<String>> = HashMap::new();

fn main() {
}
